╔════════════════════════════════════════════════════════════════╗
║           MONGODB ATLAS IP WHITELIST CONFIGURATION            ║
╚════════════════════════════════════════════════════════════════╝

📍 YOUR CURRENT IP ADDRESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IP Address: 223.185.37.232
Location:   Bhopal, Madhya Pradesh, India
ISP:        Bharti Airtel Ltd.


🔧 STEP-BY-STEP INSTRUCTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. MongoDB Atlas should open in your browser automatically
   
2. Login with your MongoDB credentials

3. Click on "Network Access" in the left sidebar
   (Under SECURITY section)

4. Click the "ADD IP ADDRESS" button

5. Choose ONE of these options:

   OPTION A - For Development (Recommended for now):
   ┌─────────────────────────────────────────────────────────┐
   │ IP Address:  0.0.0.0/0                                  │
   │ Description: Development - Allow All                    │
   │                                                         │
   │ ⚠️  This allows access from ANY IP address             │
   │    Use ONLY for development, NOT for production!       │
   └─────────────────────────────────────────────────────────┘

   OPTION B - For Production (More Secure):
   ┌─────────────────────────────────────────────────────────┐
   │ IP Address:  223.185.37.232                             │
   │ Description: Bhopal Office - Development Machine        │
   │                                                         │
   │ ✅ This only allows YOUR current IP address            │
   │    More secure, but needs update if IP changes         │
   └─────────────────────────────────────────────────────────┘

6. Click "Confirm"

7. Wait 1-2 minutes for changes to propagate


✅ VERIFICATION STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After whitelisting, run this command to verify:

  curl http://localhost:3000/api/admin/services

BEFORE Fix (You'll see this):
  {"success":false,"error":"Could not connect to any servers..."}

AFTER Fix (You should see this):
  {"success":true,"data":[...],"count":X}


🚀 QUICK TEST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run the comprehensive test script:
  
  ./test-comprehensive.sh

All API endpoints should now show ✓ PASSED


📝 NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

• If using mobile hotspot or dynamic IP, use Option A (0.0.0.0/0)
• If using static broadband IP, use Option B (223.185.37.232)
• Changes take 1-2 minutes to propagate
• You can add multiple IPs if needed
• For production deployment, whitelist only server IPs


🆘 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Still getting 500 errors after whitelisting?

1. Wait 2-3 minutes and try again
2. Check MongoDB Atlas cluster is running (not paused)
3. Verify connection string in .env.local is correct
4. Restart the dev server:
   - Stop: Ctrl+C
   - Start: npm run dev
5. Check MongoDB Atlas dashboard for connection attempts

